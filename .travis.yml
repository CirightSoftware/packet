sudo: required
dist: layout1

language: node_js
node_js:
  - '4.5'

before_script:
  - npm install -g grunt-cli
  - npm install -g bower
  - echo -e "Host github.com\n\tStrictHostKeyChecking no\n" >> ~/.ssh/config
  - npm install
  - bower install -f

script:
  - grunt build:layout1
