sudo: required
dist: layout1

language: node_js
node_js:
  - '4.5'

install:
  - npm install -g grunt-cli
  - npm install -g bower

before_script:
  - npm install
  - bower install

script:
  - grunt build:layout1
